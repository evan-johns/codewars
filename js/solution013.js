// Return "Even" or "Odd" accordingly for any given integer, positive or negative.

function even_or_odd(number) {
    if(number % 2 === 0) {
      return "Even" 
    } 
    else {
      return "Odd"
    } 
  }